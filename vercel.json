{
    "framework": "nextjs",
    "buildCommand": "npx prisma generate && next build",
    "devCommand": "next dev",
    "installCommand": "npm install --legacy-peer-deps",
    "functions": {
        "app/api/**": {
            "maxDuration": 30
        }
    },
    "regions": [
        "fra1"
    ]
}